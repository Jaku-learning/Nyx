const { SlashCommandBuilder, EmbedBuilder } = require('discord.js');

module.exports = {
	name: 'embargo',
	description: 'WIP',
	server: true,
	devOnly: true,
	callback: async (client, interaction) => {

	}
};